<UserControl x:Class="Messager.Views.ChatUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Messager.Views"
             xmlns:viewModels="clr-namespace:Messager.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="390" d:DesignWidth="474"
             d:DataContext="{d:DesignInstance Type=viewModels:ChatUCViewModel}">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>
            <TextBox/>
            <Button Grid.Column="1"/>
        </Grid>
    </Grid>
</UserControl>
